# Copy this file to values-secrets.yaml and fill in your values.
# values-secrets.yaml is gitignored and will NOT be committed.

secrets:
  AZURE_OPENAI_API_KEY: ""
  AZURE_OPENAI_ENDPOINT: "https://your-resource.openai.azure.com/"
  AZURE_OPENAI_DEPLOYMENT: "gpt-5.2"
  AZURE_OPENAI_API_VERSION: "2025-04-01-preview"
  OLLAMA_BASE_URL: ""
  OLLAMA_API_KEY: ""
  OLLAMA_MODEL: "gpt-oss:120b"
  AUTH_SECRET: ""        # Generate with: openssl rand -base64 32
  AUTH_KEYCLOAK_SECRET: ""

env:
  AUTH_ROLE_ADMIN: "itg-admin"
  AUTH_ROLE_PHD: "chair-member"
  AUTH_ROLE_STUDENT: "thesis-students"

postgresql:
  password: ""           # Generate with: openssl rand -base64 24

image:
  tag: "latest"
